pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                cleanWs()
                echo 'Building a new laptop...'
                sh 'mkdir -p build'
            }
        }
    }
    
    post {
        success {
         archiveArtifacts artifacts: 'build/**'   
        }
    }
}
